# OCPP 스펙 문서 — CC BY-ND 4.0, 재배포 조건이 걸려 있어 저장소에 포함하지 않는다.
# https://openchargealliance.org/download-ocpp/ 에서 무료로 받을 수 있다.
docs/spec/

# Gradle / JVM
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
*.class

# zannabi-code 실행 증거 (필요 시 선별 커밋)
.zannabi/

# H2 — OUT_TIMED_OUT 장부 (PLAN §5.3). 런타임 데이터라 저장소에 넣지 않는다.
data/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Kotlin 2.x 빌드 세션
.kotlin/

# 중첩 실행이 만든 로컬 권한 설정
.claude/settings.local.json

# 테스트 JVM 부산물
.attach_pid*

# 저장소에 올리지 않는 작업 메모 (지원 전략·JD 대응 등). notes/README.md 참조.
# ⚠️ 한 번 커밋되면 히스토리에 남는다. 여기 있는 것을 docs/ 로 옮기지 않는다.
notes/
